export function LogsPage() {
  return (
    <div style={{padding:'32px'}}>
      <h1 style={{fontSize:'var(--text-xl)',fontWeight:700,color:'var(--color-text)',marginBottom:'8px'}}>Logs</h1>
      <p style={{color:'var(--color-text-muted)',fontSize:'var(--text-sm)'}}>Activity logs and agent execution history.</p>
    </div>
  )
}
