export function MemoryPage() {
  return (
    <div style={{padding:'32px'}}>
      <h1 style={{fontSize:'var(--text-xl)',fontWeight:700,color:'var(--color-text)',marginBottom:'8px'}}>Memory</h1>
      <p style={{color:'var(--color-text-muted)',fontSize:'var(--text-sm)'}}>Long-term memory and knowledge stored by Chief.</p>
    </div>
  )
}
