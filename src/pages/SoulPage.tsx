export function SoulPage() {
  return (
    <div style={{padding:'32px'}}>
      <h1 style={{fontSize:'var(--text-xl)',fontWeight:700,color:'var(--color-text)',marginBottom:'8px'}}>Soul</h1>
      <p style={{color:'var(--color-text-muted)',fontSize:'var(--text-sm)'}}>Chief's personality, values and behavioral guidelines.</p>
    </div>
  )
}
