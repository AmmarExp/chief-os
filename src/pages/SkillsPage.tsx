export function SkillsPage() {
  return (
    <div style={{padding:'32px'}}>
      <h1 style={{fontSize:'var(--text-xl)',fontWeight:700,color:'var(--color-text)',marginBottom:'8px'}}>Skills</h1>
      <p style={{color:'var(--color-text-muted)',fontSize:'var(--text-sm)'}}>Tools and integrations available to your agents.</p>
    </div>
  )
}
